# Copy to .env and fill in. NEVER commit .env with a real key.
PORT=3001
CLIENT_ORIGIN=http://localhost:5173
# Server-side only — used for AI case generation (step 6). Never sent to clients.
ANTHROPIC_API_KEY=
